import {
  ANIMATION,
  BREAKPOINTS,
  SPACE_PROPERTIES,
  FONTS,
  FONT_SIZE_PROPERTIES,
} from "./tokens";

const base = {
  fonts: FONTS,
  fontSizes: FONT_SIZE_PROPERTIES,
  breakpoints: BREAKPOINTS,
  animation: ANIMATION,
  space: SPACE_PROPERTIES,
}

export default base