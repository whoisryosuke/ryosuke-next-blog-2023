---
title: Intro to Visualizing Audio Programming
date: "2024-02-26"
tags: ["blender", "ui", "tips"]
cover_image: "./how-i-made-the-render-buddy-blender-plugin.png"
---

import dynamic from "next/dynamic";
export const BlogMDXWrapper = dynamic(
  () => import("@components/dom/BlogMDXWrapper/BlogMDXWrapper"),
  { ssr: false }
);
export default function Layout({children}) {
  const frontmatter = {
    title: "Intro to Visualizing Audio Programming",
    date: "2024-03-12",
    section: "blog",
    tags: ["blender", "ui", "tips"],
    cover_image: "./how-i-made-the-render-buddy-blender-plugin.png",
  }
  return <BlogMDXWrapper frontMatter={frontmatter} slug="test-mdx">{children}</BlogMDXWrapper>;

}

export const P5Viz = dynamic(
  () => import("@components/dom/P5Viz/P5Viz"),
  { ssr: false }
);
 
# MIDI

export function TestViz() {
  let time = 0;
    const canvasSize = { 
        x: 500,
        y: 500,
    }
    const sketch = (p) => {
    if(window === 'undefined') return;
    let y = 100;
    p.setup = () => {
    console.log("setup canvas");
    p.createCanvas(canvasSize.x, canvasSize.y);
    p.stroke(255); // Set line drawing color to white
    p.frameRate(30);
    };
    p.draw = () => {
    if(window === 'undefined') return;
    // console.log('drawing!!')
    p.background("#333"); // Set the background to black
    
    p.fill("#008080");
    const distance = 10;
    const speed = 120; // larger = slower
    const offsetX = p.sin(p.millis() / speed) * distance; 
    const offsetY = p.cos(p.millis() / speed) * distance; 
    p.ellipse(canvasSize.x / 2 + offsetX, canvasSize.y / 2 + offsetY, 72, 72);

        p.pop();
        }

    };

    return <P5Viz title="Test Viz" sketch={sketch} width={canvasSize.x} height={canvasSize.y} />

}

<TestViz />
